platform :ios, '10.0'

target 'Example' do
    use_frameworks!
    pod 'EthereumKitSwift', :path => './'
    
    target 'EthereumKitSwiftTests' do
        inherit! :search_paths
        pod 'Nimble'
    end
end
